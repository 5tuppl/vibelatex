# Frontend Libraries

VibeLaTeX Desktop loads CodeMirror 6 and PDF.js from CDNs in `src/main.js`.

To run fully offline, place local ESM builds in this folder and update the import
URLs at the top of `src/main.js`.
