# Frontend Libraries

VibeLaTeX loads CodeMirror 6 and PDF.js from CDNs in `public/app.js`.

To run fully offline, place local ESM builds in this folder and update the import
URLs at the top of `public/app.js`.
